import 'dart:developer';

import 'package:flutter/material.dart';

class ExampleFireStore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Example Firestore"),
      ),
      body: Center(
        child: null,
      ),
    );
  }
}
